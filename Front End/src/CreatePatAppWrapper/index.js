import React from 'react';
import PatientWrapper from '../PatientWrapper';
import CreateAppointment from '../CreateAppointment';
const CreatePatAppWrapper = () => {
    return ( <PatientWrapper>
        <CreateAppointment />
    </PatientWrapper> );
}

export default CreatePatAppWrapper;